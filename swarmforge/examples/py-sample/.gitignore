.venv/
__pycache__/
.pytest_cache/
*.pyc
# lockfiles generated during testing — never commit
uv.lock
poetry.lock
Pipfile.lock

# swarmforge runtime registry (per-target, never committed)
.swarmforge/
